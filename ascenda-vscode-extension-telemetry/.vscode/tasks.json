{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ascenda: build (shared + shell)",
      "type": "shell",
      "command": "npm --prefix .. run build:shared && npm run compile",
      "group": "build",
      "problemMatcher": "$tsc"
    },
    { "type": "npm", "script": "compile", "group": "build", "problemMatcher": "$tsc", "label": "npm: compile", "detail": "tsc -p ./" },
    { "type": "npm", "script": "watch", "group": "build", "problemMatcher": "$tsc-watch", "label": "npm: watch", "detail": "tsc -watch -p ./" }
  ]
}
