{
  "name": "ai-engineer-tools",
  "private": true,
  "version": "0.0.1",
  "description": "Ascenda developer telemetry and pairing tooling monorepo",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "ascenda-vscode-extension-telemetry",
    "ascenda-cursor-extension",
    "ascenda-claude-code-hooks",
    "ascenda-codex-hooks",
    "ascenda-pairing-sim"
  ],
  "scripts": {
    "build": "npm run build:shared && npm run build:tools",
    "build:shared": "npm run compile -w @ascenda/tool-contract && npm run compile -w @ascenda/tool-kit && npm run compile -w @ascenda/ide-extension-core",
    "build:tools": "npm run compile -w ascenda-vscode && npm run compile -w ascenda-cursor && npm run build -w ascenda-claude-code-hooks && npm run build -w ascenda-codex-hooks && npm run build -w ascenda-pairing-sim",
    "package": "npm run package -w ascenda-vscode && npm run package -w ascenda-cursor",
    "verify": "./scripts/check-dry.sh && npm run test",
    "test": "npm run build && npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
